const intinalState = {
    add: {
        title: 'Blabla bla',
        content: '#'
    }
};

export default (state = intinalState, action) => {
    switch(action.type){
        default:
            return state;
    }
}
