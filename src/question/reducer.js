const intinalState = {
    add: {
        title: '',
        content: '## markdown me ^_^ ',
        tags: ''
    }
};

export default (state = intinalState, action) => {
    switch(action.type){
        default:
            return state;
    }
}
